pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                echo 'Simple pipeline configured with Jenkinsfile and GitHub Webhook.'
                sh 'echo Integrating Jenkins Pipeline with GitHub Webhook using Jenkinsfile'
            }
        }
    }
}
